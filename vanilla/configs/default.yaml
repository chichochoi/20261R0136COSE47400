seed: 42
image_size: 224
num_classes: 3
latent_dim: 128
batch_size: 32
num_workers: 0
val_ratio: 0.2
learning_rate: 0.0001
epochs: 50
beta_kl: 1.0
kl_warmup_epochs: 5
# No classifier guidance — intentional baseline design
# classifier_loss_weight: 0.0
